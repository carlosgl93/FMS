import 'package:flutter/material.dart';

class RedemptionControl extends StatelessWidget {
  const RedemptionControl({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
          appBar: AppBar(
            title: Text('Canjes en proceso')
          ),
          body: Center(
            child: Text('Canjes en proceso'),
            ),
    );
  }
}